Compares a scene sampling a box area light (path_scene_area_emitter) with one using the spherical rectangles of the visible faces of the box (path_scene_aabb_emitter).

Both converge, but the spherical one with much less variance
